# React Component - Holberton School

Project directory for the React component tasks.

This module starts from the last task of the React Props project and converts components progressively to class components while keeping the dashboard behavior unchanged.

## Task 0

Convert the `App` component from a function component to a class component.
